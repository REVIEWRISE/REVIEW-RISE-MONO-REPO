generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["driverAdapters"]
  binaryTargets   = ["native", "linux-musl-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
}

model Account {
  id                String  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  userId            String  @db.Uuid
  type              String
  provider          String
  providerAccountId String
  refresh_token     String?
  access_token      String?
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String?
  session_state     String?
  user              User    @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
}

model Session {
  id           String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  sessionToken String   @unique
  userId       String   @db.Uuid
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model User {
  id                String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name              String?
  email             String?            @unique
  emailVerified     DateTime?
  image             String?
  password          String?
  createdAt         DateTime           @default(now())
  updatedAt         DateTime           @updatedAt
  accounts          Account[]
  auditLogs         AuditLog[]
  reviewReplies     ReviewReply[]
  seoSnapshots      SeoSnapshot[]
  sessions          Session[]
  userBusinessRoles UserBusinessRole[]
  userRoles         UserRole[]
  adriseSessions    AdriseSession[]
  creativeEngineAnalytics CreativeEngineAnalytics[]
  gbpAppliedRecommendations BrandRecommendation[] @relation("AppliedRecommendationUser")
  gbpSuggestionActivities    GbpSuggestionActivity[]
}

model UserRole {
  id     String @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  userId String @db.Uuid
  roleId String @db.Uuid
  role   Role   @relation(fields: [roleId], references: [id], onDelete: Cascade)
  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, roleId])
  @@index([userId])
  @@index([roleId])
}

model Business {
  id                   String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name                 String
  slug                 String                @unique
  description          String?
  phone                String?
  email                String?
  website              String?
  status               String                @default("active")
  createdAt            DateTime              @default(now())
  updatedAt            DateTime              @updatedAt
  deletedAt            DateTime?
  aiVisibilityMetrics  AIVisibilityMetric[]
  dna                  BrandDNA?
  brandProfiles        BrandProfile[]
  recommendations      BrandRecommendation[]
  brandScores          BrandScore[]
  competitors          Competitor[]
  competitorReviews    CompetitorReview[]
  contentIdeas         ContentIdea[]
  jobs                 Job[]
  keywords             Keyword[]
  locations            Location[]
  opportunitiesReports OpportunitiesReport[]
  reports              Report[]
  reviews              Review[]
  reviewSyncLogs       ReviewSyncLog[]
  scheduledPosts       ScheduledPost[]
  socialConnections    SocialConnection[]
  subscriptions        Subscription[]
  userBusinessRoles    UserBusinessRole[]
  visibilityMetrics    VisibilityMetric[]
  visibilitySnapshots  VisibilitySnapshot[]
  posts                Post[]
  captionDrafts        CaptionDraft[]
  imagePrompts         ImagePrompt[]
  scriptDrafts         ScriptDraft[]
  carouselDrafts       CarouselDraft[]
  plannerPlans         MonthlyPlannerPlan[]
  adriseSessions       AdriseSession[]
  creativeEngineAnalytics CreativeEngineAnalytics[]
  creativeConcepts     CreativeConcept[]
  gbpProfileSnapshots  GbpProfileSnapshot[]
  gbpSuggestionActivities GbpSuggestionActivity[]
  napMasterRecords     NapMasterRecord[]
  reportsCenterConfig  ReportsCenterConfig?
  reportsCenterSchedules ReportsCenterSchedule[]
  reportsCenterRuns    ReportsCenterRun[]
  reportsCenterExportJobs ReportsCenterExportJob[]

  @@index([name])
  @@index([status])
}

model Location {
  id                  String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name                String
  address             String?
  timezone            String?
  platformIds         Json?
  tags                String[]
  lastSync            DateTime?
  status              String               @default("active")
  businessId          String               @db.Uuid
  createdAt           DateTime             @default(now())
  updatedAt           DateTime             @updatedAt
  deletedAt           DateTime?
  aiVisibilityMetrics AIVisibilityMetric[]
  competitorReviews   CompetitorReview[]
  jobs                Job[]
  keywords            Keyword[]
  business            Business             @relation(fields: [businessId], references: [id], onDelete: Cascade)
  reviews             Review[]
  reviewSources       ReviewSource[]
  reviewSyncLogs      ReviewSyncLog[]
  scheduledPosts      ScheduledPost[]
  socialConnections   SocialConnection[]
  userBusinessRoles   UserBusinessRole[]
  visibilityMetrics   VisibilityMetric[]
  pendingGoogleConnections  PendingGoogleConnection[]
  platformIntegrations PlatformIntegration[]
  photos              LocationPhoto[]
  lastPhotoSyncAt     DateTime?
  gbpProfileSnapshots  GbpProfileSnapshot[]
  metrics              LocationMetric[]
  competitors          LocationCompetitor[]
  metricJobs           MetricJob[]
  gbpRecommendations   BrandRecommendation[]
  gbpSuggestionActivities GbpSuggestionActivity[]
  napMasterRecord      NapMasterRecord?

  @@index([businessId])
}

model NapMasterRecord {
  id          String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId  String   @db.Uuid
  locationId  String   @unique @db.Uuid
  name        String?
  address     String?
  phone       String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  business    Business @relation(fields: [businessId], references: [id], onDelete: Cascade)
  location    Location @relation(fields: [locationId], references: [id], onDelete: Cascade)

  @@index([businessId])
}

model Role {
  id                String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name              String             @unique
  description       String?
  createdAt         DateTime           @default(now())
  updatedAt         DateTime           @updatedAt
  permissions       RolePermission[]
  userBusinessRoles UserBusinessRole[]
  userRoles         UserRole[]
}

model Permission {
  id          String           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  action      String           @unique
  description String?
  createdAt   DateTime         @default(now())
  updatedAt   DateTime         @updatedAt
  roles       RolePermission[]
}

model RolePermission {
  id           String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  roleId       String     @db.Uuid
  permissionId String     @db.Uuid
  permission   Permission @relation(fields: [permissionId], references: [id], onDelete: Cascade)
  role         Role       @relation(fields: [roleId], references: [id], onDelete: Cascade)

  @@unique([roleId, permissionId])
  @@index([roleId])
  @@index([permissionId])
}

model UserBusinessRole {
  id         String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  userId     String    @db.Uuid
  businessId String    @db.Uuid
  roleId     String    @db.Uuid
  createdAt  DateTime  @default(now())
  updatedAt  DateTime  @updatedAt
  deletedAt  DateTime?
  locationId String?   @db.Uuid
  business   Business  @relation(fields: [businessId], references: [id], onDelete: Cascade)
  location   Location? @relation(fields: [locationId], references: [id])
  role       Role      @relation(fields: [roleId], references: [id], onDelete: Cascade)
  user       User      @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, businessId, roleId, locationId])
  @@index([userId])
  @@index([businessId])
  @@index([locationId])
}

model Subscription {
  id                   String    @id @default(uuid())
  businessId           String    @db.Uuid
  plan                 String
  status               String
  currentPeriodEnd     DateTime
  stripeSubscriptionId String?   @unique
  stripeCustomerId     String?
  trialEndsAt          DateTime?
  canceledAt           DateTime?
  createdAt            DateTime  @default(now())
  updatedAt            DateTime  @updatedAt
  deletedAt            DateTime?
  business             Business  @relation(fields: [businessId], references: [id], onDelete: Cascade)

  @@index([businessId])
  @@index([status])
  @@index([stripeSubscriptionId])
}

model PasswordResetToken {
  id        String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  email     String
  token     String   @unique
  expires   DateTime
  createdAt DateTime @default(now())

  @@index([email])
}

model AuditLog {
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  userId     String?  @db.Uuid
  action     String
  entityType String
  entityId   String   @db.Uuid
  details    Json?
  createdAt  DateTime @default(now())
  user       User?    @relation(fields: [userId], references: [id], onDelete: Cascade)
  gbpProfileSnapshots GbpProfileSnapshot[]

  @@index([userId])
  @@index([entityId])
  @@index([createdAt])
}

model GbpProfileSnapshot {
  id                 String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId         String              @db.Uuid
  locationId         String              @db.Uuid
  captureType        String              @default("sync")
  snapshot           Json
  changedFields      Json?
  diffBaseSnapshotId String?             @db.Uuid
  auditLogId         String?             @db.Uuid
  suggestionRefs     Json?
  capturedAt         DateTime            @default(now())
  createdAt          DateTime            @default(now())
  business           Business            @relation(fields: [businessId], references: [id], onDelete: Cascade)
  location           Location            @relation(fields: [locationId], references: [id], onDelete: Cascade)
  auditLog           AuditLog?           @relation(fields: [auditLogId], references: [id], onDelete: SetNull)
  diffBaseSnapshot   GbpProfileSnapshot? @relation("GbpSnapshotDiffBase", fields: [diffBaseSnapshotId], references: [id], onDelete: SetNull)
  diffChildren       GbpProfileSnapshot[] @relation("GbpSnapshotDiffBase")
  audit              GbpProfileAudit?
  recommendations    BrandRecommendation[]

  @@index([businessId])
  @@index([locationId])
  @@index([capturedAt])
  @@index([auditLogId])
}

model GbpProfileAudit {
  id              String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  snapshotId      String             @unique @db.Uuid
  score           Float              // 0-100
  details         Json               // Full breakdown of the audit
  createdAt       DateTime           @default(now())
  updatedAt       DateTime           @updatedAt
  snapshot        GbpProfileSnapshot @relation(fields: [snapshotId], references: [id], onDelete: Cascade)

  @@index([snapshotId])
}

model ReviewSyncLog {
  id            String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId    String    @db.Uuid
  locationId    String    @db.Uuid
  platform      String
  status        String
  errorMessage  String?
  errorStack    String?
  reviewsSynced Int       @default(0)
  durationMs    Int?
  requestData   Json?
  responseData  Json?
  jobId         String?   @db.Uuid
  startedAt     DateTime
  completedAt   DateTime?
  createdAt     DateTime  @default(now())
  business      Business  @relation(fields: [businessId], references: [id], onDelete: Cascade)
  job           Job?      @relation(fields: [jobId], references: [id])
  location      Location  @relation(fields: [locationId], references: [id], onDelete: Cascade)

  @@index([businessId])
  @@index([locationId])
  @@index([platform])
  @@index([status])
  @@index([createdAt])
}

model EmailVerificationToken {
  id        String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  email     String
  token     String   @unique
  expires   DateTime
  createdAt DateTime @default(now())

  @@index([email])
}

model SeoSnapshot {
  id              String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  url             String
  healthScore     Int
  categoryScores  Json
  recommendations Json
  seoElements     Json
  userId          String?  @db.Uuid
  createdAt       DateTime @default(now())
  user            User?    @relation(fields: [userId], references: [id])

  @@index([userId])
  @@index([url])
}

model Keyword {
  id           String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId   String        @db.Uuid
  locationId   String?       @db.Uuid
  keyword      String
  searchVolume Int?
  difficulty   Float?
  tags         String[]
  status       String        @default("active")
  createdAt    DateTime      @default(now())
  updatedAt    DateTime      @updatedAt
  deletedAt    DateTime?
  business     Business      @relation(fields: [businessId], references: [id], onDelete: Cascade)
  location     Location?     @relation(fields: [locationId], references: [id])
  ranks        KeywordRank[]

  @@index([businessId])
  @@index([locationId])
  @@index([status])
}

model KeywordRank {
  id                 String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  keywordId          String   @db.Uuid
  rankPosition       Int?
  mapPackPosition    Int?
  hasFeaturedSnippet Boolean  @default(false)
  hasPeopleAlsoAsk   Boolean  @default(false)
  hasLocalPack       Boolean  @default(false)
  hasKnowledgePanel  Boolean  @default(false)
  hasImagePack       Boolean  @default(false)
  hasVideoCarousel   Boolean  @default(false)
  rankingUrl         String?
  searchLocation     String?
  device             String   @default("desktop")
  capturedAt         DateTime @default(now())
  createdAt          DateTime @default(now())
  keyword            Keyword  @relation(fields: [keywordId], references: [id], onDelete: Cascade)

  @@index([keywordId])
  @@index([capturedAt])
}

model VisibilityMetric {
  id                   String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId           String    @db.Uuid
  locationId           String?   @db.Uuid
  periodStart          DateTime
  periodEnd            DateTime
  periodType           String
  mapPackAppearances   Int       @default(0)
  totalTrackedKeywords Int       @default(0)
  mapPackVisibility    Float     @default(0)
  top3Count            Int       @default(0)
  top10Count           Int       @default(0)
  top20Count           Int       @default(0)
  shareOfVoice         Float     @default(0)
  featuredSnippetCount Int       @default(0)
  localPackCount       Int       @default(0)
  computedAt           DateTime  @default(now())
  createdAt            DateTime  @default(now())
  business             Business  @relation(fields: [businessId], references: [id], onDelete: Cascade)
  location             Location? @relation(fields: [locationId], references: [id])

  @@unique([businessId, locationId, periodStart, periodEnd, periodType])
  @@index([businessId])
  @@index([locationId])
  @@index([periodStart, periodEnd])
}

model Job {
  id             String          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  type           String
  status         String
  businessId     String?         @db.Uuid
  locationId     String?         @db.Uuid
  payload        Json?
  result         Json?
  error          Json?
  retryCount     Int             @default(0)
  maxRetries     Int             @default(3)
  createdAt      DateTime        @default(now())
  updatedAt      DateTime        @updatedAt
  startedAt      DateTime?
  completedAt    DateTime?
  failedAt       DateTime?
  business       Business?       @relation(fields: [businessId], references: [id])
  location       Location?       @relation(fields: [locationId], references: [id])
  reviewSyncLogs ReviewSyncLog[]

  @@index([type])
  @@index([status])
  @@index([businessId])
  @@index([locationId])
  @@index([createdAt])
}

model BrandProfile {
  id                    String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId            String                 @db.Uuid
  websiteUrl            String
  status                String                 @default("pending")
  currentExtractedData  Json?
  createdAt             DateTime               @default(now())
  updatedAt             DateTime               @updatedAt
  description           String?
  title                 String?
  tone                  Json?
  autoReplySettings     Json?
  assets                BrandAsset[]
  colors                BrandColor[]
  fonts                 BrandFont[]
  pages                 BrandPage[]
  business              Business               @relation(fields: [businessId], references: [id], onDelete: Cascade)
  socialLinks           BrandSocialLink[]
  extractedDataVersions ExtractedDataVersion[]

  @@index([businessId])
  @@index([status])
}

model ExtractedDataVersion {
  id             String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  brandProfileId String       @db.Uuid
  versionNumber  Int
  extractedData  Json
  createdAt      DateTime     @default(now())
  brandProfile   BrandProfile @relation(fields: [brandProfileId], references: [id], onDelete: Cascade)

  @@index([brandProfileId])
}

model BrandAsset {
  id             String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  brandProfileId String       @db.Uuid
  type           String
  url            String
  altText        String?
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  brandProfile   BrandProfile @relation(fields: [brandProfileId], references: [id], onDelete: Cascade)

  @@index([brandProfileId])
}

model BrandColor {
  id             String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  brandProfileId String       @db.Uuid
  hexCode        String
  type           String
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  brandProfile   BrandProfile @relation(fields: [brandProfileId], references: [id], onDelete: Cascade)

  @@index([brandProfileId])
}

model BrandFont {
  id             String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  brandProfileId String       @db.Uuid
  family         String
  usage          String
  url            String?
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  brandProfile   BrandProfile @relation(fields: [brandProfileId], references: [id], onDelete: Cascade)

  @@index([brandProfileId])
}

model BrandPage {
  id             String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  brandProfileId String       @db.Uuid
  type           String
  url            String
  summary        String?
  content        String?
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  brandProfile   BrandProfile @relation(fields: [brandProfileId], references: [id], onDelete: Cascade)

  @@index([brandProfileId])
}

model BrandSocialLink {
  id             String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  brandProfileId String       @db.Uuid
  platform       String
  url            String
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  brandProfile   BrandProfile @relation(fields: [brandProfileId], references: [id], onDelete: Cascade)

  @@index([brandProfileId])
}

model FeatureFlag {
  id                String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name              String   @unique
  description       String?
  isEnabled         Boolean  @default(false)
  rolloutPercentage Int      @default(100)
  rules             Json?
  owner             String?
  tags              String[]
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@index([name])
}

model SystemSetting {
  id          String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  key         String   @unique
  value       Json
  description String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([key])
}

model AIVisibilityMetric {
  id                String           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId        String           @db.Uuid
  locationId        String?          @db.Uuid
  visibilityScore   Float            @default(0)
  sentimentScore    Float            @default(0)
  shareOfVoice      Float            @default(0)
  citationAuthority Float            @default(0)
  periodStart       DateTime
  periodEnd         DateTime
  periodType        String
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt
  platformData      AIPlatformData[]
  business          Business         @relation(fields: [businessId], references: [id], onDelete: Cascade)
  location          Location?        @relation(fields: [locationId], references: [id])

  @@index([businessId])
  @@index([locationId])
  @@index([periodStart, periodEnd])
}

model AIPlatformData {
  id                   String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  aIVisibilityMetricId String             @db.Uuid
  platform             String
  rank                 Int?
  sentiment            String
  mentioned            Boolean            @default(false)
  sourcesCount         Int                @default(0)
  responseSnippet      String?
  capturedAt           DateTime           @default(now())
  aIVisibilityMetric   AIVisibilityMetric @relation(fields: [aIVisibilityMetricId], references: [id], onDelete: Cascade)

  @@index([aIVisibilityMetricId])
  @@index([platform])
}

model Competitor {
  id             String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId     String               @db.Uuid
  name           String
  website        String?
  domain         String?
  type           CompetitorType       @default(UNKNOWN)
  source         String?
  relevanceScore Float?
  ranking        Int?
  isUserAdded    Boolean              @default(false)
  isHidden       Boolean              @default(false)
  createdAt      DateTime             @default(now())
  updatedAt      DateTime             @updatedAt
  business       Business             @relation(fields: [businessId], references: [id], onDelete: Cascade)
  snapshots      CompetitorSnapshot[]

  @@unique([businessId, name])
  @@unique([businessId, domain])
  @@index([businessId])
  @@index([type])
}

model CompetitorSnapshot {
  id                String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  competitorId      String     @db.Uuid
  headline          String?
  uvp               String?
  serviceList       String[]
  pricingCues       String[]
  trustSignals      Json?
  ctaStyles         String[]
  contentCategories String[]
  differentiators   Json?
  whatToLearn       String[]
  whatToAvoid       String[]
  techStack         Json?
  trafficMetrics    Json?
  metrics           Json?
  capturedAt        DateTime   @default(now())
  competitor        Competitor @relation(fields: [competitorId], references: [id], onDelete: Cascade)

  @@index([competitorId])
  @@index([capturedAt])
}

model VisibilitySnapshot {
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId String   @db.Uuid
  range      String
  score      Float
  breakdown  Json
  capturedAt DateTime @default(now())
  business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  @@index([businessId])
  @@index([capturedAt])
}

model Report {
  id          String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId  String   @db.Uuid
  title       String
  version     String
  htmlContent String
  pdfUrl      String?
  generatedBy String?  @db.Uuid
  generatedAt DateTime @default(now())
  business    Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  @@index([businessId])
  @@index([generatedAt])
}

model ReportsCenterConfig {
  id           String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId   String   @unique @db.Uuid
  preset       String
  sections     Json
  sectionOrder Json
  whiteLabel   Json
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  business     Business @relation(fields: [businessId], references: [id], onDelete: Cascade)
  schedule     ReportsCenterSchedule?
  runs         ReportsCenterRun[]

  @@index([businessId])
}

model ReportsCenterSchedule {
  id              String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId      String   @db.Uuid
  configId        String   @unique @db.Uuid
  frequency       String
  recipients      Json
  triggerEnabled  Boolean  @default(false)
  seoDropThreshold Int     @default(5)
  nextRunAt       DateTime
  lastRunAt       DateTime?
  status          String   @default("active")
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  business        Business @relation(fields: [businessId], references: [id], onDelete: Cascade)
  config          ReportsCenterConfig @relation(fields: [configId], references: [id], onDelete: Cascade)

  @@index([businessId])
  @@index([nextRunAt])
}

model ReportsCenterRun {
  id               String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId       String   @db.Uuid
  configId         String   @db.Uuid
  name             String
  type             String
  status           String
  generatedAt      DateTime @default(now())
  deliveredAt      DateTime?
  openedAt         DateTime?
  pdfUrl           String?
  webUrl           String?
  shareToken       String?  @unique
  sharePasswordHash String?
  meta             Json?
  business         Business @relation(fields: [businessId], references: [id], onDelete: Cascade)
  config           ReportsCenterConfig @relation(fields: [configId], references: [id], onDelete: Cascade)

  @@index([businessId])
  @@index([generatedAt])
  @@index([status])
}

model ReportsCenterExportJob {
  id          String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId  String   @db.Uuid
  type        String
  status      String
  resultUrl   String?
  error       Json?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  completedAt DateTime?
  business    Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  @@index([businessId])
  @@index([status])
  @@index([createdAt])
}

model OpportunitiesReport {
  id                String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId        String    @db.Uuid
  positioningMap    Json
  gaps              Json
  strategies        Json
  suggestedTaglines String[]
  contentIdeas      Json
  generatedAt       DateTime  @default(now())
  expiresAt         DateTime?
  shareToken        String?   @unique
  pdfUrl            String?
  business          Business  @relation(fields: [businessId], references: [id], onDelete: Cascade)

  @@index([businessId])
  @@index([shareToken])
}

model BrandDNA {
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId String   @unique @db.Uuid
  values     String[]
  voice      String?
  audience   String?
  mission    String?
  business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)
}

model ContentIdea {
  id          String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId  String   @db.Uuid
  title       String
  description String?
  platform    String
  category    String?
  status      String   @default("draft")
  createdAt   DateTime @default(now())
  business    Business @relation(fields: [businessId], references: [id], onDelete: Cascade)
  posts       Post[]

  @@index([businessId])
}

model Review {
  id             String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId     String        @db.Uuid
  locationId     String?       @db.Uuid
  platform       String
  externalId     String
  author         String
  rating         Int
  content        String?
  publishedAt    DateTime
  response       String?
  respondedAt    DateTime?
  createdAt      DateTime      @default(now())
  aiSuggestions  Json?
  replyError     String?
  replyStatus    String?
  reviewSourceId String?       @db.Uuid
  sentiment      String?
  tags           String[]      @default([])
  business       Business      @relation(fields: [businessId], references: [id], onDelete: Cascade)
  location       Location?     @relation(fields: [locationId], references: [id])
  reviewSource   ReviewSource? @relation(fields: [reviewSourceId], references: [id])
  replies        ReviewReply[]

  @@unique([platform, externalId])
  @@index([businessId])
}

model ReviewReply {
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  reviewId   String   @db.Uuid
  content    String
  authorType String
  sourceType String
  status     String   @default("draft")
  externalId String?
  userId     String?  @db.Uuid
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
  review     Review   @relation(fields: [reviewId], references: [id], onDelete: Cascade)
  user       User?    @relation(fields: [userId], references: [id])

  @@index([reviewId])
  @@index([status])
}

model ReviewSource {
  id                    String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  locationId            String    @db.Uuid
  platform              String
  status                String    @default("active") // active, error, disconnected
  createdAt             DateTime  @default(now())
  updatedAt             DateTime  @updatedAt
  reviews               Review[]
  location              Location  @relation(fields: [locationId], references: [id], onDelete: Cascade)

  @@unique([locationId, platform])
  @@index([locationId])
  @@index([status])
}

model PlatformIntegration {
  id                    String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  locationId            String    @db.Uuid
  platform              String
  accessToken           String?   // AES-256-GCM encrypted (format: iv:authTag:ciphertext)
  refreshToken          String?   // AES-256-GCM encrypted
  expiresAt             BigInt?
  status                String    @default("active") // active, error, disconnected
  metadata              Json?
  gbpAccountId          String?   // e.g. "accounts/123456"
  gbpLocationName       String?   // e.g. "accounts/123456/locations/789"
  gbpLocationTitle      String?   // e.g. "My Awesome Cafe"
  connectedAt           DateTime?
  createdAt             DateTime  @default(now())
  updatedAt             DateTime  @updatedAt
  location              Location  @relation(fields: [locationId], references: [id], onDelete: Cascade)

  @@unique([locationId, platform])
  @@index([locationId])
  @@index([status])
}

model PendingGoogleConnection {
  id                    String   @id @default(uuid())
  locationId            String   @db.Uuid
  nonce                 String   @unique
  encryptedAccessToken  String
  encryptedRefreshToken String
  expiryDate            BigInt
  accountsJson          Json     // List of GBP accounts [{name, accountName, type}]
  locationsJson         Json     // List of GBP locations [{name, title, address, ...}]
  expiresAt             DateTime // Short TTL (~10 minutes)
  createdAt             DateTime @default(now())
  location              Location @relation(fields: [locationId], references: [id], onDelete: Cascade)

  @@index([locationId])
  @@index([expiresAt])
}

model BrandRecommendation {
  id            String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId    String    @db.Uuid
  locationId    String?   @db.Uuid
  source        String?   @default("general")
  lifecycleState String   @default("SAVED")
  auditSnapshotId String? @db.Uuid
  auditFindingCodes String[] @default([])
  category      String
  title         String
  description   String
  why           String[]
  steps         String[]
  impact        String
  effort        String
  confidence    Float     @default(0)
  priorityScore Float     @default(0)
  kpiTarget     Json?
  status        String    @default("open")
  notes         String?
  generatedAt   DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  dismissedAt   DateTime?
  completedAt   DateTime?
  appliedAt     DateTime?
  appliedByUserId String? @db.Uuid
  appliedNotes  String?
  rejectedAt    DateTime?
  rejectedReason String?
  business      Business  @relation(fields: [businessId], references: [id], onDelete: Cascade)
  location      Location? @relation(fields: [locationId], references: [id], onDelete: SetNull)
  auditSnapshot GbpProfileSnapshot? @relation(fields: [auditSnapshotId], references: [id], onDelete: SetNull)
  appliedByUser User? @relation("AppliedRecommendationUser", fields: [appliedByUserId], references: [id], onDelete: SetNull)
  activities    GbpSuggestionActivity[]

  @@index([businessId])
  @@index([locationId])
  @@index([source])
  @@index([lifecycleState])
  @@index([auditSnapshotId])
  @@index([category])
  @@index([status])
  @@index([priorityScore])
  @@index([generatedAt])
}

model GbpSuggestionActivity {
  id               String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  recommendationId String              @db.Uuid
  businessId       String              @db.Uuid
  locationId       String              @db.Uuid
  userId           String?             @db.Uuid
  action           String
  notes            String?
  details          Json?
  createdAt        DateTime            @default(now())
  recommendation   BrandRecommendation @relation(fields: [recommendationId], references: [id], onDelete: Cascade)
  business         Business            @relation(fields: [businessId], references: [id], onDelete: Cascade)
  location         Location            @relation(fields: [locationId], references: [id], onDelete: Cascade)
  user             User?               @relation(fields: [userId], references: [id], onDelete: SetNull)

  @@index([recommendationId])
  @@index([businessId])
  @@index([locationId])
  @@index([action])
  @@index([createdAt])
}

model LocationPhoto {
  id              String    @id
  locationId      String    @db.Uuid
  accountId       String?
  googleUrl       String
  thumbnailUrl    String
  category        String?
  createTime      DateTime?
  updateTime      DateTime?
  sourceUrl       String?
  attribution     String?
  mediaFormat     String    @default("PHOTO")
  lastSyncedAt    DateTime  @default(now())
  hash            String?

  location        Location  @relation(fields: [locationId], references: [id], onDelete: Cascade)

  @@index([locationId])
  @@index([category])
}

model BrandScore {
  id                   String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId           String   @db.Uuid
  visibilityScore      Float    @default(0)
  trustScore           Float    @default(0)
  consistencyScore     Float    @default(0)
  visibilityBreakdown  Json
  trustBreakdown       Json
  consistencyBreakdown Json
  computedAt           DateTime @default(now())
  periodStart          DateTime
  periodEnd            DateTime
  business             Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  @@unique([businessId, periodStart, periodEnd])
  @@index([businessId])
  @@index([computedAt])
}

model CompetitorReview {
  id             String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId     String    @db.Uuid
  locationId     String?   @db.Uuid
  competitorName String
  averageRating  Float
  totalReviews   Int
  source         String
  capturedAt     DateTime  @default(now())
  createdAt      DateTime  @default(now())
  business       Business  @relation(fields: [businessId], references: [id], onDelete: Cascade)
  location       Location? @relation(fields: [locationId], references: [id])

  @@index([businessId])
  @@index([locationId])
  @@index([capturedAt])
}

model SocialConnection {
  id           String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId   String    @db.Uuid
  locationId   String?   @db.Uuid
  platform     String    // 'facebook', 'instagram', 'linkedin'
  pageId       String?   // Facebook Page ID or LinkedIn Organization ID
  pageName     String?   // Display name
  profileId    String?   // Instagram Business Account ID
  accessToken  String    // Encrypted
  refreshToken String?   // Encrypted
  tokenExpiry  DateTime?
  scopes       String[]  // Granted OAuth scopes
  status       String    @default("active") // active, error, revoked
  errorMessage String?
  lastSyncAt   DateTime?
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
  business     Business  @relation(fields: [businessId], references: [id], onDelete: Cascade)
  location     Location? @relation(fields: [locationId], references: [id])

  @@unique([businessId, locationId, platform, pageId])
  @@index([businessId])
  @@index([locationId])
  @@index([platform])
  @@index([status])
}

enum CompetitorType {
  DIRECT_LOCAL
  CONTENT
  AGGREGATOR
  UNKNOWN
}

model ScheduledPost {
  id             String          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId     String          @db.Uuid
  locationId     String?         @db.Uuid
  platforms      String[]        // e.g., ['facebook', 'instagram', 'linkedin']
  content        String?
  media          Json?           // { images: string[], videos: string[], carousel: any }
  scheduledAt    DateTime
  timezone       String          @default("UTC")
  status         String          @default("draft") // draft, scheduled, publishing, published, failed, canceled
  isRecurring    Boolean         @default(false)
  recurrenceRule String?         // RRULE string for future option
  parentPostId   String?         @db.Uuid
  createdAt      DateTime        @default(now())
  updatedAt      DateTime        @updatedAt

  business       Business        @relation(fields: [businessId], references: [id], onDelete: Cascade)
  location       Location?       @relation(fields: [locationId], references: [id])
  parentPost     ScheduledPost?  @relation("RecurringPosts", fields: [parentPostId], references: [id])
  childPosts     ScheduledPost[] @relation("RecurringPosts")
  publishingJobs PublishingJob[]

  @@index([businessId])
  @@index([locationId])
  @@index([scheduledAt])
  @@index([status])
}

model PublishingJob {
  id              String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  scheduledPostId String        @db.Uuid
  platform        String
  status          String        @default("pending") // pending, processing, completed, failed
  externalId      String?       // Platform-specific post ID
  error           String?
  attemptCount    Int           @default(0)
  lastAttemptAt   DateTime?
  createdAt      DateTime      @default(now())
  updatedAt      DateTime      @updatedAt

  scheduledPost   ScheduledPost @relation(fields: [scheduledPostId], references: [id], onDelete: Cascade)

  @@index([scheduledPostId])
  @@index([platform])
  @@index([status])
}

model Post {
  id          String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId  String   @db.Uuid
  content     String?
  mediaUrls   String[]
  platform    String   // 'facebook', 'instagram', 'linkedin', etc.
  scheduledAt DateTime?
  status      String   @default("draft") // draft, scheduled, published
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  business    Business @relation(fields: [businessId], references: [id], onDelete: Cascade)
  idea        ContentIdea? @relation(fields: [ideaId], references: [id])
  ideaId      String? @db.Uuid
  
  @@index([businessId])
  @@index([status])
  @@index([scheduledAt])
}

model CaptionDraft {
  id          String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId  String   @db.Uuid
  content     String
  platform    String
  tone        String?
  status      String   @default("generated") // generated, saved
  createdAt   DateTime @default(now())
  
  business    Business @relation(fields: [businessId], references: [id], onDelete: Cascade)
}

model ImagePrompt {
  id          String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId  String   @db.Uuid
  prompt      String
  style       String?
  aspectRatio String?
  createdAt   DateTime @default(now())
  
  business    Business @relation(fields: [businessId], references: [id], onDelete: Cascade)
  images      GeneratedImage[]
}

model GeneratedImage {
  id          String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  promptId    String   @db.Uuid
  url         String
  storagePath String?
  metadata    Json?
  createdAt   DateTime @default(now())
  
  prompt      ImagePrompt @relation(fields: [promptId], references: [id], onDelete: Cascade)
}

model ScriptDraft {
  id          String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId  String   @db.Uuid
  title       String?
  platform    String
  hook        String?
  body        String?
  cta         String?
  duration    String?
  createdAt   DateTime @default(now())
  
  business    Business @relation(fields: [businessId], references: [id], onDelete: Cascade)
}

model CarouselDraft {
  id          String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId  String   @db.Uuid
  topic       String
  slides      Json     // Array of { title, content, imagePrompt }
  status      String   @default("draft")
  createdAt   DateTime @default(now())

  business    Business @relation(fields: [businessId], references: [id], onDelete: Cascade)
}

model ContentTemplate {
  id          String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  industry    String   // e.g., 'Local Restaurant', 'Salon', 'Agency', 'Real Estate'
  objective   String   // e.g., 'Awareness', 'Engagement', 'Sales'
  contentType String   // e.g., 'image', 'carousel', 'video'
  title       String
  content     String
  mediaPrompt String?
  tags        String[]
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([industry])
  @@index([objective])
}

model SeasonalEvent {
  id          String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  date        DateTime
  market      String   @default("Global") // e.g., 'US', 'Global', 'Local'
  region      String?
  name        String
  description String?
  tags        String[]
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([date])
  @@index([market])
}

model MonthlyPlannerPlan {
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId String   @db.Uuid
  month      Int      // 1-12
  year       Int
  industry   String
  status     String   @default("draft") // draft, generated, converted
  config     Json     // { platforms: string[], frequency: string, businessType: string }
  days       Json     // Array of { day: number, contentIdea: string, contentType: string, platforms: string[], seasonalHook?: string, templateId?: string }
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  business   Business @relation(fields: [businessId], references: [id], onDelete: Cascade)

  @@unique([businessId, month, year])
  @@index([businessId])
}

model AdriseSession {
  id            String               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId    String               @db.Uuid
  userId        String?              @db.Uuid
  status        String               @default("active") // active, completed, failed
  mode          String               @default("QUICK") // QUICK, PRO
  industryCode  String?
  objective     String?
  budgetMonthly Float?
  geo           Json?
  inputs        Json?
  createdAt     DateTime             @default(now())
  updatedAt     DateTime             @updatedAt
  
  business      Business             @relation(fields: [businessId], references: [id], onDelete: Cascade)
  user          User?                @relation(fields: [userId], references: [id])
  versions      AdriseSessionVersion[]
  outputs       AdriseOutput[]

  @@index([businessId])
  @@index([userId])
  @@index([status])
}

model AdriseSessionVersion {
  id               String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  sessionId        String        @db.Uuid
  versionNumber    Int
  inputs           Json
  createdAt        DateTime      @default(now())
  
  session          AdriseSession @relation(fields: [sessionId], references: [id], onDelete: Cascade)

  @@index([sessionId])
}

model AdriseOutput {
  id          String        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  sessionId   String        @db.Uuid
  output      Json
  status      String        @default("success") // success, partial, failed
  createdAt   DateTime      @default(now())
  
  session     AdriseSession @relation(fields: [sessionId], references: [id], onDelete: Cascade)

  @@index([sessionId])
}

model CreativeEngineAnalytics {
  id          String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  eventType   String // wizard_start, wizard_complete, plan_exported
  businessId  String?  @db.Uuid
  userId      String?  @db.Uuid
  metadata    Json?
  createdAt   DateTime @default(now())
  business    Business? @relation(fields: [businessId], references: [id], onDelete: Cascade)
  user        User?     @relation(fields: [userId], references: [id], onDelete: SetNull)

  @@index([businessId])
  @@index([userId])
  @@index([eventType])
  @@index([createdAt])
}

model CreativeConcept {
  id            String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  businessId    String   @db.Uuid
  headline      String
  visualIdea    String
  primaryText   String
  cta           String
  imagePrompt   String?
  imageUrl      String?
  formatPrompts Json
  tone          Json?
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  
  business      Business @relation(fields: [businessId], references: [id], onDelete: Cascade)
  
  @@index([businessId])
}

model LocationMetric {
  id                    String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  locationId            String   @db.Uuid
  date                  DateTime @db.Date
  impressionsTotal      Int      @default(0)
  impressionsDiscovery  Int      @default(0)
  impressionsDirect     Int      @default(0)
  photoViews            Int      @default(0)
  visibilityScore       Float?
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt

  location              Location @relation(fields: [locationId], references: [id], onDelete: Cascade)

  @@unique([locationId, date])
  @@map("location_metrics")
}

model LocationCompetitor {
  id                   String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  locationId           String   @db.Uuid
  competitorName       String
  rating               Float?
  reviewCount          Int?
  photoCount           Int?
  estimatedVisibility  Float?
  createdAt            DateTime @default(now())

  location             Location @relation(fields: [locationId], references: [id], onDelete: Cascade)

  @@index([locationId])
  @@map("location_competitors")
}

model MetricJob {
  id             String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  locationId     String    @db.Uuid
  jobType        String
  status         String
  errorMessage   String?
  startedAt      DateTime  @default(now())
  finishedAt     DateTime?

  location       Location  @relation(fields: [locationId], references: [id], onDelete: Cascade)

  @@index([locationId, jobType, status])
  @@map("metric_jobs")
}
